import numpy as np


def reflect(input):
    """Mirror an image horizontally (left <-> right).

    Parameters
    ----------
    input : (H, W) or (H, W, C) array

    Returns
    -------
    Array of the same shape with the column order reversed.
    """
    # TODO: reverse the order of the columns (axis 1).
    raise NotImplementedError("Implement reflect")
